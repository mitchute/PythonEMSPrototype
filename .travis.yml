
#os: linux
#language: python
#python: 3.4
#
#addons:
#  apt:
#    packages:
#    - python3.4-dev
#
#script:
#- mkdir build && cd build && cmake .. && make
#
#after_success:
#- cd build && ./EmbeddedPython


language: cpp

sudo: required

services:
  - docker

env: SH="docker exec -t ubuntu-test bash -c"

before_install:
  - docker run -d --name ubuntu-test -e LC_ALL="en_US.UTF-8" -e LANG="en_US.UTF-8" -v $(pwd):/travis -w /travis ubuntu:latest tail -f /dev/null
  - docker ps

install:
 - $SH "apt-get update"
 - $SH "apt-get install -y locales"
 - $SH "locale-gen en_US.UTF-8"
 - $SH "apt-get install -y python3.6-dev cmake build-essential"

script:
 - $SH "mkdir build && cd build && cmake .. && make && ./EmbeddedPython"
